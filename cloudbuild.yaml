options:
  logging: CLOUD_LOGGING_ONLY

steps:
- name: 'gcr.io/cloud-builders/gcloud'
  args:
  - compute
  - ssh
  - shivaprasad-cicd-vm
  - --zone=us-central1-a
  - --command=cd ~ && rm -rf my-cicd-app && git clone https://github.com/ShivaprasadSAcharya/my-cicd-app.git && sudo cp my-cicd-app/index.html /var/www/html/index.html
